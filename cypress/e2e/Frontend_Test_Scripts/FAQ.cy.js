// /// <reference types="cypress"/>
// // /import * as cy from "core-
// //tests going to the FAQ page
//
//
// it('FAQ page testing', function () {
//      cy.visit(Cypress.env("MEMORIAL_MATRIX_URL"))
//      cy.get('[data-cy="resources"]').click({force:true})
//       cy.get('[data-cy="faq_page"]').should('be.visible').click({ multiple: true, force: true })
// });