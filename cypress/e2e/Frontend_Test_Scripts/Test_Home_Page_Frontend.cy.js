//this test script tests the functionality of the home page.
//Create a cypress.env.json file in your root of your project and populate all username, URLs, and passwords in that file.
//To change the environment that this test script runs in you will need to change the baseurl in the cypress.env.json file


// it('verify title of page', function(){
// cy.visit(Cypress.env("MEMORIAL_MATRIX_URL"))
//     cy.title().should('eq','Memorial Matri')
// })

// it('Test Menu Bar Items', function () {
//     cy.visit(Cypress.env("MEMORIAL_MATRIX_URL"))
    // cy.get('[data-cy="submit_mem"]').click({ multiple: true, force: true })
    // cy.get('[data-cy="search_memorial"]').click({ multiple: true, force: true })
    // cy.get('[data-cy="memorial_map"]').click({ multiple: true, force: true })
    // cy.get('[data-cy="resources"]').click({force:true})
    // cy.get('[data-cy="about_us"]').click({ multiple: true, force: true })
    // cy.get('[data-cy="resources"]').click({force:true})
    // cy.get('[data-cy="faq_page"]').click({ multiple: true, force: true })
    // cy.get('[data-cy="resources"]').click({force:true})
    // cy.get('[data-cy="mbc_contact_us"]').click({ multiple: true, force: true })
    // cy.get('[data-cy="mbc_donate"]').click({ multiple: true, force: true })
    // cy.get('[data-cy="sponsors"]').click({ multiple: true, force: true })
    // cy.get('[data-cy="home"]').click()
    // cy.get('[data-cy="memorials"]').click()
    // cy.go('back')
    // cy.get('[data-cy="submit_memorial"]').click()
    // cy.go('back')
    // cy.get('[data-cy="donate"]').click()
    // cy.go('back')

// })